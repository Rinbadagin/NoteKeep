class DecryptedNote{
     constructor(decryptedMessage,title,timeSubmitted,index){
          this.decryptedMessage=decryptedMessage;
          this.title=title;
          this.timeSubmitted=timeSubmitted;
          this.index=index;
     }
}